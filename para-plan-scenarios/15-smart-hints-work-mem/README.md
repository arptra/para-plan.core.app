# Подсказка: низкий work_mem

**Что не так:** сортировка большой таблицы при слишком маленьком `work_mem`.

**Что обнаружит `/api/sql-hints`:** рекомендация увеличить `work_mem` и подсказка заменить `SELECT *`.

## Как проверить
```bash
docker-compose up -d
./demo.sh
```
