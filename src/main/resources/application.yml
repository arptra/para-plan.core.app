
server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/demo
    username: paraplan
    password: paraplan
    hikari:
      maximum-pool-size: 4
  jackson:
    serialization:
      INDENT_OUTPUT: true

logging:
  level:
    root: INFO
    dev.paraplan: DEBUG
