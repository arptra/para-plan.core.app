{
  "sql": "UPDATE orders SET amount = amount * 1.01 WHERE status='PAID' AND created_at >= now() - interval '3 days';",
  "options": {
    "enableLandscape": true,
    "enableDcc": true,
    "mcSamples": 25
  }
}