{
  "sql": "SELECT region_id, sum(amount) FROM customers c JOIN orders o ON o.customer_id=c.id GROUP BY ROLLUP (region_id);",
  "options": {
    "enableLandscape": true,
    "enableDcc": true,
    "mcSamples": 25
  }
}