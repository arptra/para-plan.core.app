{
  "sql": "SELECT c.region_id, COUNT(*) cnt, SUM(o.amount) sum_a FROM customers c JOIN orders o ON o.customer_id=c.id GROUP BY c.region_id HAVING SUM(o.amount) > 1000 ORDER BY sum_a DESC;",
  "options": {
    "enableLandscape": true,
    "enableDcc": true,
    "mcSamples": 25
  }
}