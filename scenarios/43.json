{
  "sql": "SELECT customer_id, array_agg(id ORDER BY created_at DESC) FROM orders GROUP BY customer_id LIMIT 50;",
  "options": {
    "enableLandscape": true,
    "enableDcc": true,
    "mcSamples": 25
  }
}