{
  "sql": "SELECT DISTINCT ON (o.customer_id) o.customer_id, o.id, o.amount FROM orders o ORDER BY o.customer_id, o.amount DESC;",
  "options": {
    "enableLandscape": true,
    "enableDcc": true,
    "mcSamples": 25
  }
}